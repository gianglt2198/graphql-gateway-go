extend type Query {
  category(id: ID!): CategoryEntity!

  categories(
    after: Cursor
    first: Int
    before: Cursor
    last: Int
    orderBy: [CategoryOrder!]
    where: CategoryFilter
  ): CategoryPaginatedConnection!
}
